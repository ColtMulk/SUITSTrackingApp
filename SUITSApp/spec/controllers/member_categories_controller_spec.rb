require 'rails_helper'

RSpec.describe MemberCategoriesController, :type => :controller do

  it "should only let one category be default for new users" do
    
  end

  it "should only let one category be default for old users" do
    
  end

end
